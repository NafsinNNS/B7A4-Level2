import { Router } from "express";

const router = Router();

router.post("/create",)
router.post("/confirm",)
router.get("/payments",)
router.get("/payments/:id",)

export const paymentRoutes = router;